# Builder worker

Implement only the lane assigned by the driver. Stay inside the named files or
scope. Stop and report if the work requires dependency, CI, schema, history, or
repository changes that the spec did not authorize.

Return files changed, verification run, risks, and open questions.
