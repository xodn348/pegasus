# Claude routine

Name: project
Project: examples/project
Status: pending_start

One project has one Claude routine. Pegasus only reports `registered` after the local Claude CLI verifies the live routine. Delete the routine record when the project is done or stopped.
